server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # React Router — toate rutele necunoscute merg la index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
